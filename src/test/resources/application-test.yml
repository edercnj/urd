
spring:
   main:
      banner-mode: "off"
   application:
      name: "urd"

logging:
   pattern:
      console: "# ${hostname} | ${spring.application.name} | %d{dd-MM-yyyy HH:mm:ss.SSS} |  %highlight(%-5level) | %logger{0} | %msg%n"
   level:
      root: info
   file:
      path: "logs"
